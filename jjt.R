#问题3-1：整型，浮点型，复数型
#问题3-2：levels()
#问题3-3：使用函数as.numeric(“6.283185”)
#问题3-4：summary(),edit(),dim(),view()
#问题3-5：使用函数rm(list=ls())

#练习3-1
#类
class(Inf)
class(NA)
class(NaN)
class("")
#类型
typeof(Inf)
typeof(NA)
typeof(NaN)
typeof("")
#模式
mode(Inf)
mode(NA)
mode(NaN)
mode("")
#存储模式
storage.mode(Inf)
storage.mode(NA)
storage.mode(NaN)
storage.mode("")

#练习3-2
#随即从dog,cat,hamster,goldfish中以相等的概率生成1000个宠物名
radom<-sample(c("dog","cat","hamster","goldfish"),1000,replace=TRUE)
#显示抽样后的前十个宠物
radom[1:10]
#计算每种宠物的数量
table(radom)

#练习3-3
apple<-1
banana<-2
pear<-3
lemon<-4
#列出用户工作区中所有包含字母a的变量
ls(pattern=".*[a].*")

#问题4-1：c(0,0.25,0.5,0.75,1)
#问题4-2：names();直接命名
#问题4-3：向量名称索引；正（负）整数索引；逻辑形索引；零值
#问题4-4：
len4<-array(dim=c(3,4,5))
length(len4)
#长度为60
#问题4-5：A%*%B
#问题4-6：
pro1<-seq(11,50,by=3)
pro1-c(2,3)
#问题4-7：
pro2<-seq(1.1,4,by=20)
pro2%*%c(4,5,8,9)
#问题4-8：
c(1,2,4,6)+c(8,0,-1,-4)
c(1,2,4,6)-c(8,0,-1,-4)
c(1,2,4,6)%*%t(c(8,0,-1,-4))
c(1,2,4,6)%/%c(8,0,-1,-4)

#练习4-1：
ex1<-c(1:20)*(c(1:20)+1)/2
names(ex1)<-letters[1:20]
ex1[c("a","e","i","o","u")]
#练习4-2：
diag(c(11:1,0:11))


#问题5-1：
lengths(list(alpha=1,list(beta=2,gamma=3,delta=4),eta=NULL))
#问题5-2：pairlists
#问题5-3：data.frame()
#问题5-4：data.frame(...,check.name=FALSE)
#问题5-5：横向合并：cbind(),merge();纵向合并：rbind()

#练习5-1：
list1<-list(c(1,4,9),16,25,36,49,NA,64,NA,81,NA)
#练习5-2：
frame1<-iris[-5]
colMeans(frame1)
#练习5-3：
xbeaver1<-cbind(id=1,beaver1)
xbeaver2<-cbind(id=2,beaver2)
mixbeaver<-rbind(xbeaver1,xbeaver2)
subset(mixbeaver,activ==1)
